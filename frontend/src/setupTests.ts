/**
 * setupTests.ts
 * -------------
 * Test setup file for React Testing Library and Jest.
 *
 * - Imports '@testing-library/jest-dom' to add custom matchers for DOM assertions.
 * - Enables assertions like toHaveTextContent, toBeInTheDocument, etc.
 * - Automatically loaded before running tests.
 */
// jest-dom adds custom jest matchers for asserting on DOM nodes.
// allows you to do things like:
// expect(element).toHaveTextContent(/react/i)
// learn more: https://github.com/testing-library/jest-dom
import '@testing-library/jest-dom';
